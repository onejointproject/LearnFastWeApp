
   const state = {
       text:"HOME这里是store文件夹下的home.js的值"
   }
   const mutations = {

   }
   const actions = {

   }
   const getters = {

   }

   export default {
      state,
      mutations,
      actions,
      getters, 
    }
